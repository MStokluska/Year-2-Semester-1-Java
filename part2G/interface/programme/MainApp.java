package programme;

import javax.swing.JFrame;

public class MainApp {

	public static void main(String[] args) {
		new LoginWindow();
	}
}
// this app purpouse is to calculate area, perimeter, combined area and combined perimeter of 3 different objects
// circle, triangle and rectangle in a window. To be able to log in, user must generate id and password, user can also
// access setup to change color of the background.